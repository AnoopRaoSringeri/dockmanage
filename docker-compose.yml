# Main compose file that includes all services
# Split into separate files for better organization:
# - docker-compose.app.yml: node and nginx services
# - docker-compose.dockmanage.yml: dockmanage orchestrator
# - docker-compose.certbot.yml: certbot service

include:
  - docker-compose.app.yml
  - docker-compose.dockmanage.yml
  - docker-compose.certbot.yml
